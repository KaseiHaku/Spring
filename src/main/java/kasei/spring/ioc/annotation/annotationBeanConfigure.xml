<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/context
		    http://www.springframework.org/schema/context/spring-context.xsd  
		    http://www.springframework.org/schema/beans  
		    http://www.springframework.org/schema/beans/spring-beans.xsd">
	
	<!-- 如果需要测试，请把该文件拷贝到 src 目录下，在包内无法访问 -->
	
	
	<!-- ################## 通过注解配置 bean  #############################
	 	导入spring-aop-4.3.5.RELEASE.jar
		Spring 能够从classpath 下自动扫描、侦测和实例化具有下列注解的组件，
		实例化对象的引用名为类的首字母小写.
		
		注解类的注解
		@Component:基本注解，标识了一个受Spring IOC容器管理的组件
		@Repository:标识持久层组件
		@Service：标识服务层（业务层）组件
		@Controller：标识表现层组件
	
	
		<context:component-scan>：指定spring IOC容器扫描的包  
		base-package:指定一个需要scan 的基类包，Spring容器将会 扫描该包及其子包中所有带注解的类，扫描多个包使用逗号分隔
		resource-pattern:指定扫描的资源
		
		<context:component-scan>标签会自动注册 Autowired	AnnotationBeanPostProcessor 实例，
		该实例自动装配具有 @Autowired @Resource 和@Inject 注解的属性（这3个注解可以标到属性上也可以标到set方法上）
		如果有多个匹配的bean 则出异常
		@Autowired(required=false) 自动装配具有兼容属性的 bean 可以注解字段和方法 ,允许某个属性不被注入则required=false
		@Qualifier("userJdbcRepository")  指定注入 bean id="userJdbcRepository" 的 bean
	 ##################################################################-->
	
	
	<!-- 配置 Spring 扫描的包 -->
	<!-- resource-pattern="repository/*.class" -> 表示只扫描 
			a02annotation.b01beanConfigure.repository/*.class 下的所有 .class 文件
	<context:component-scan 
			base-package="a02annotation.b01beanConfigure" 
			resource-pattern="repository/*.class"></context:component-scan>
	-->
	<context:component-scan base-package="kasei.spring.ioc.annotation">
		<!-- 不包含  @Controller 注释的类
		<context:exclude-filter type="annotation" 
				expression="org.springframework.stereotype.Controller"/>
		-->		
		<!-- 只包含  @Controller 注释的类：需要设置 context:component-scan 属性 use-default-filters="false" 	
		<context:include-filter type="annotation" 
				expression="org.springframework.stereotype.Controller"/>
		-->	
		<!-- 不包含 assigned 指定的类
		<context:exclude-filter type="assignable" 
				expression="a02annotation.b01beanConfigure.B03Service"/>
		-->
	</context:component-scan>
	
	
	<!-- 地表最强扫包配置实例 ：使用该文件时记得注释掉 -->
	<context:component-scan base-package="kasei.spring.ioc.annotation, kasei.spring.ioc.xml" use-default-filters="false">
		<!-- 设置只包含 ？ 注解注释的类，需要设置 context:component-scan 属性 use-default-filters="false" -->
		<context:include-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
		<context:include-filter type="annotation" expression="org.springframework.stereotype.Component"/>
		
		<!-- 不包含 assigned 指定的类 -->
		<context:exclude-filter type="assignable" expression="kasei.spring.ioc.annotation.Excluded"/>
		
		<!-- 不包含以下注解注释的类 -->
		<context:exclude-filter type="annotation" expression="org.springframework.stereotype.Service"/>
		<context:exclude-filter type="annotation" expression="org.springframework.stereotype.Repository"/>
	</context:component-scan>
	
	
	
	
</beans>